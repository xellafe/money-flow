---
phase: 8
slug: v1-cleanup
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-27
---

# Phase 08 — UI Design Contract

> Visual and interaction contract for Phase 08: v1.0 Cleanup.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Scope Note

This is a **tech debt cleanup phase**. Of the 5 items in scope, only **1 produces a visible UI change**:

| Item | Type | UI Impact |
|------|------|-----------|
| Add `cursor-pointer` to 2 buttons in `SettingsView.jsx` | CSS micro-fix | ✅ Visual — pointer cursor on hover |
| Remove dead state from `useFilters.js` / `useModals.js` | Code cleanup | ❌ None |
| Remove dead `selectedYear === null` branches in `App.jsx` | Code cleanup | ❌ None |
| Normalize `AddTransactionModal` import to barrel pattern | Code cleanup | ❌ None |
| Fix REQUIREMENTS.md documentation | Docs cleanup | ❌ None |

All design contract sections below describe the **existing system**, not new additions. The only change being introduced is the cursor-pointer class on the two SettingsView buttons.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | STATE.md — shadcn explicitly excluded 2026-03-17 |
| Preset | not applicable | — |
| Component library | Radix UI primitives | STATE.md decision |
| Icon library | Lucide React | Observed in Sidebar, AppHeader |
| Font | Inter Variable (Fontsource local bundle) | src/index.css `@theme` |

---

## Spacing Scale

Declared values (multiples of 4) — **established in Phase 1, no changes in Phase 8**:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

**SettingsView button padding (unchanged):** `px-4 py-2` = 16px horizontal / 8px vertical — consistent with sm/md scale.

Exceptions: `--spacing-sidebar: 240px` / `--spacing-sidebar-collapsed: 64px` (structural, not content spacing)

---

## Typography

Established in Phase 1 — **no changes in Phase 8**:

**Permitted weights: 2 — `500 (medium)` and `600 (semibold)`.**

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Display | 24px (text-2xl) | 600 (semibold) | 1.2 | `text-2xl font-semibold` |
| Heading | 14px (text-sm) | 500 (medium) | 1.4 | `text-sm font-medium uppercase tracking-wider` |
| Body | 16px (text-base) | 500 (medium) | 1.5 | `text-base font-medium` |
| Label / Button | 14px (text-sm) | 500 (medium) | 1.4 | `text-sm font-medium` |

> **Weight consolidation note:** Body weight collapsed from 400 → 500 to stay within the 2-weight contract. The Inter Variable typeface at weight 500 is visually indistinguishable from 400 at body size (16px); no perceived change to users. Existing `text-base` body elements should carry `font-medium` to match this contract.

SettingsView uses Display (h2 "Impostazioni"), Heading (h3 section labels), and Label (button text) — all already consistent.

---

## Color

Established in Phase 1 via `src/index.css @theme` — **no new colors in Phase 8**:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `gray-50` (#f9fafb) | Page background, main content surface |
| Secondary (30%) | `gray-100` (#f3f4f6) | Cards, SettingsView buttons (bg-gray-100), section backgrounds |
| Accent (10%) | `brand-600` (#2563eb) | Primary CTA buttons, active nav items, links |
| Destructive | `expense-500` (#f43f5e) | Destructive action buttons only (ConfirmModal) |

Accent reserved for: primary action buttons, active sidebar navigation state, focused form elements. **Not used in SettingsView** — SettingsView buttons are secondary-style (gray-100 background).

---

## Visual Hierarchy — SettingsView

**Primary visual anchor:** The `<h2>` page heading "Impostazioni" (`text-2xl font-semibold`) draws the eye first; section cards follow reading order top-to-bottom (Categories → Sync), each introduced by an uppercase `text-sm font-medium` section label before its action button.

**Focal-point statement:** The heading is the single dominant element on the view. No competing focal points exist — both action buttons are equal-weight secondary-style (gray-100 bg) and sit below their respective section labels, reinforcing a predictable F-pattern scan path: heading → section label → button → section label → button.

---

## The Single UI Change: cursor-pointer

**Requirement:** UX-06 — "Cursore pointer su tutti gli elementi cliccabili" (REQUIREMENTS.md, Phase 7 → backfilled in Phase 8)

**Target elements:**
- `SettingsView.jsx` line 19 — "Gestione Categorie" button
- `SettingsView.jsx` line 31 — "Sincronizzazione Drive" button

**Before:**
```
className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors"
```

**After:**
```
className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors cursor-pointer"
```

**Class added:** `cursor-pointer` — standard Tailwind utility, no new tokens, no visual change beyond cursor affordance.

**Verification:** All other interactive elements in the app (AppHeader buttons, Sidebar nav items, TransactionRow actions) already carry `cursor-pointer`. SettingsView was the sole outlier per the Phase 7 audit.

---

## Copywriting Contract

Phase 8 introduces no new copy. Existing button labels are documented here as the canonical contract:

| Element | Copy | Language |
|---------|------|----------|
| Settings page heading | "Impostazioni" | Italian |
| Categories section label | "Categorie" | Italian |
| Categories button CTA | "Gestione Categorie" | Italian (unchanged) |
| Sync section label | "Sincronizzazione" | Italian |
| Sync button CTA | "Sincronizzazione Drive" | Italian (unchanged) |

**Empty state:** Not applicable — SettingsView always shows both sections regardless of data state.

**Error state:** Not applicable — SettingsView buttons open modals via callbacks; no error display in SettingsView itself.

**Destructive actions:** None in this phase. All destructive flows remain in ConfirmModal (unchanged by this phase).

---

## Interaction Contract

| Element | Current Behavior | After Phase 8 |
|---------|-----------------|---------------|
| "Gestione Categorie" button | Click opens CategoryManager modal; cursor defaults to `default` (system arrow) | Click opens CategoryManager modal; cursor shows `pointer` on hover |
| "Sincronizzazione Drive" button | Click opens SyncSettings modal; cursor defaults to `default` (system arrow) | Click opens SyncSettings modal; cursor shows `pointer` on hover |

**Hover transition:** `transition-colors` already applied (150ms) — hover bg shifts from `gray-100` to `gray-200`. No change to this transition.

**Focus behavior:** Unchanged — browser default focus ring applies (not modified in this phase).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No registry additions in this phase.

---

## Non-UI Changes (for executor reference)

These items are **out of scope for visual review** but documented for completeness:

| File | Change | Visual Impact |
|------|--------|---------------|
| `src/hooks/useFilters.js` | Remove 3 dead state pairs: `dashboardTypeFilter`, `expandedCategory`, `showCategoryPercentage` | None |
| `src/hooks/useModals.js` | Remove 1 dead state pair: `openDropdown` | None |
| `src/App.jsx` | Remove `selectedYear === null` ternary guards (4 locations) | None |
| `src/components/modals/AddTransactionModal.jsx` | Normalize import: `ModalShell` from direct path → named barrel `{ ModalShell } from '../ui'` | None |
| `.planning/REQUIREMENTS.md` | FOUND-04: table status "Pending" → "Complete"; FOUND-11: `[ ]` → `[x]` with inline closure note | Documentation only |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

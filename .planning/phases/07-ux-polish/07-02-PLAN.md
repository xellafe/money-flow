---
phase: 07-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modals/AddTransactionModal.jsx
  - src/components/layout/AppHeader.jsx
autonomous: true
requirements:
  - UX-05

must_haves:
  truths:
    - "AppHeader shows 'Aggiungi' button on all views (dashboard, transactions, settings)"
    - "Clicking AppHeader button opens AddTransactionModal"
    - "AddTransactionModal has 4 form fields: date, importo, descrizione, categoria"
    - "Modal 'Aggiungi' button is disabled when description is empty"
    - "Submitting valid form creates transaction and closes modal"
  artifacts:
    - path: "src/components/modals/AddTransactionModal.jsx"
      provides: "Modal for manual transaction entry using ModalShell"
      contains: "Nuova transazione"
    - path: "src/components/layout/AppHeader.jsx"
      provides: "Always-visible Aggiungi button"
      exports: ["AppHeader"]
  key_links:
    - from: "src/components/layout/AppHeader.jsx"
      to: "App.jsx"
      via: "onAddTransaction prop"
      pattern: "onAddTransaction"
    - from: "src/components/modals/AddTransactionModal.jsx"
      to: "src/hooks/useTransactionData.js"
      via: "addManualTransaction callback"
      pattern: "onConfirm"
---

<objective>
Create AddTransactionModal component and make AppHeader "Aggiungi" button always visible.

Purpose: Enable users to manually add transactions from any view (UX-05) via a clean modal form.
Output: New AddTransactionModal using ModalShell + AppHeader button visible on all views.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ux-polish/07-CONTEXT.md
@.planning/phases/07-ux-polish/07-RESEARCH.md
@.planning/phases/07-ux-polish/07-UI-SPEC.md

<interfaces>
<!-- Key types and patterns the executor needs -->

From src/hooks/useModals.js:
```jsx
const [showAddTransaction, setShowAddTransaction] = useState(false);
const [newTransaction, setNewTransaction] = useState({
  date: '', description: '', amount: '', category: 'Altro',
});
// Both are exported from the hook
```

From src/hooks/useTransactionData.js (addManualTransaction signature):
```jsx
const addManualTransaction = useCallback(() => {
  // Reads from newTransaction state — takes NO arguments
  const { date, description, amount, category } = newTransaction;
  // Validates and creates transaction
  // Calls setShowAddTransaction(false) on success
}, [newTransaction, showToast, setNewTransaction, setShowAddTransaction]);
```

From src/components/ui/ModalShell.jsx:
```jsx
export default function ModalShell({ 
  title,      // string — modal header
  onClose,    // () => void — close handler
  size,       // 'sm' | 'md' | 'lg' — max width
  children    // ReactNode
})
```

From src/components/layout/AppHeader.jsx (current button — line 41-49):
```jsx
{view === 'transactions' && (
  <button onClick={onAddTransaction} className="...">
    <Plus size={16} />
    Aggiungi transazione
  </button>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AddTransactionModal component</name>
  <files>src/components/modals/AddTransactionModal.jsx</files>
  <read_first>
    - src/components/ui/ModalShell.jsx (wrapper API)
    - src/hooks/useModals.js (newTransaction state shape)
    - src/hooks/useTransactionData.js (addManualTransaction at line 220-251)
    - .planning/phases/07-ux-polish/07-UI-SPEC.md (AddTransactionModal section lines 162-214)
  </read_first>
  <action>
Create new file src/components/modals/AddTransactionModal.jsx:

```jsx
import { useEffect } from 'react';
import { Plus } from 'lucide-react';
import ModalShell from '../ui/ModalShell';

/**
 * Modal for manually adding a new transaction.
 * Uses ModalShell wrapper and binds to useModals newTransaction state.
 * 
 * @param {{
 *   newTransaction: { date: string, description: string, amount: string, category: string },
 *   setNewTransaction: (tx: object) => void,
 *   onConfirm: () => void,
 *   onClose: () => void,
 *   categories: string[],
 * }} props
 */
export default function AddTransactionModal({
  newTransaction,
  setNewTransaction,
  onConfirm,
  onClose,
  categories,
}) {
  // Set default date to today when modal opens
  useEffect(() => {
    if (!newTransaction.date) {
      const today = new Date().toISOString().slice(0, 10);
      setNewTransaction((prev) => ({ ...prev, date: today }));
    }
  }, []); // eslint-disable-line react-hooks/exhaustive-deps

  const handleChange = (field) => (e) => {
    setNewTransaction((prev) => ({ ...prev, [field]: e.target.value }));
  };

  const isValid = newTransaction.description?.trim().length > 0;

  // Sort categories alphabetically with 'Altro' at end
  const sortedCategories = [...categories].sort((a, b) => {
    if (a === 'Altro') return 1;
    if (b === 'Altro') return -1;
    return a.localeCompare(b, 'it');
  });

  return (
    <ModalShell title="Nuova transazione" onClose={onClose} size="sm">
      <form
        onSubmit={(e) => {
          e.preventDefault();
          if (isValid) onConfirm();
        }}
        className="flex flex-col gap-4"
      >
        {/* Data field */}
        <div>
          <label className="block text-xs font-semibold text-gray-600 mb-1">
            Data
          </label>
          <input
            type="date"
            value={newTransaction.date}
            onChange={handleChange('date')}
            className="w-full rounded-lg border border-gray-200 px-3 py-2 text-sm text-gray-800 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent"
          />
        </div>

        {/* Importo field */}
        <div>
          <label className="block text-xs font-semibold text-gray-600 mb-1">
            Importo
          </label>
          <input
            type="text"
            inputMode="decimal"
            value={newTransaction.amount}
            onChange={handleChange('amount')}
            placeholder="es. -45.50"
            className="w-full rounded-lg border border-gray-200 px-3 py-2 text-sm text-gray-800 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent"
          />
        </div>

        {/* Descrizione field */}
        <div>
          <label className="block text-xs font-semibold text-gray-600 mb-1">
            Descrizione
          </label>
          <input
            type="text"
            value={newTransaction.description}
            onChange={handleChange('description')}
            placeholder="Descrizione…"
            required
            className="w-full rounded-lg border border-gray-200 px-3 py-2 text-sm text-gray-800 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent"
          />
        </div>

        {/* Categoria field */}
        <div>
          <label className="block text-xs font-semibold text-gray-600 mb-1">
            Categoria
          </label>
          <select
            value={newTransaction.category}
            onChange={handleChange('category')}
            className="w-full rounded-lg border border-gray-200 px-3 py-2 text-sm text-gray-800 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent cursor-pointer"
          >
            {sortedCategories.map((cat) => (
              <option key={cat} value={cat}>
                {cat}
              </option>
            ))}
          </select>
        </div>

        {/* Footer buttons */}
        <div className="flex justify-end gap-2 mt-2">
          <button
            type="button"
            onClick={onClose}
            className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors cursor-pointer"
          >
            Annulla
          </button>
          <button
            type="submit"
            disabled={!isValid}
            className="bg-brand-600 hover:bg-brand-700 disabled:opacity-50 disabled:cursor-not-allowed text-white rounded-lg px-4 py-2 text-sm font-medium transition-colors cursor-pointer flex items-center gap-2"
          >
            <Plus size={16} />
            Aggiungi
          </button>
        </div>
      </form>
    </ModalShell>
  );
}
```

Key implementation notes:
- Uses ModalShell size="sm" (max-w-md) for compact form
- Form fields: date (defaults to today), importo (text with decimal inputMode), descrizione (required), categoria (select)
- addManualTransaction reads from newTransaction state — no args passed, just call onConfirm()
- Primary button disabled when description empty: `disabled={!isValid}`
- Categories sorted alphabetically with 'Altro' at end
  </action>
  <verify>
    <automated>Get-ChildItem src/components/modals/AddTransactionModal.jsx && grep -n "Nuova transazione" src/components/modals/AddTransactionModal.jsx && grep -n "ModalShell" src/components/modals/AddTransactionModal.jsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/modals/AddTransactionModal.jsx exists
    - Contains `import ModalShell from '../ui/ModalShell'`
    - Contains `<ModalShell title="Nuova transazione" onClose={onClose} size="sm">`
    - Contains `disabled={!isValid}`
    - Contains `type="date"` input with `value={newTransaction.date}`
    - Contains `placeholder="es. -45.50"` for importo field
    - Contains `required` attribute on description input
    - Contains `bg-brand-600 hover:bg-brand-700` on primary button
    - Contains `bg-gray-100 hover:bg-gray-200` on cancel button
  </acceptance_criteria>
  <done>AddTransactionModal component created with form fields (date, importo, descrizione, categoria), validation, and ModalShell wrapper</done>
</task>

<task type="auto">
  <name>Task 2: Update AppHeader to show Aggiungi button on all views</name>
  <files>src/components/layout/AppHeader.jsx</files>
  <read_first>
    - src/components/layout/AppHeader.jsx
    - .planning/phases/07-ux-polish/07-UI-SPEC.md (AppHeader wiring lines 200-206)
    - .planning/phases/07-ux-polish/07-CONTEXT.md (Decision B)
  </read_first>
  <action>
Update src/components/layout/AppHeader.jsx to make the "Aggiungi" button always visible:

1. **Remove the conditional wrapper** around the button (line 41-49 currently):

BEFORE:
```jsx
{view === 'transactions' && (
  <button
    onClick={onAddTransaction}
    className="flex items-center gap-2 px-4 py-2 bg-brand-500 text-white text-sm font-semibold rounded-lg hover:bg-brand-600 transition-colors cursor-pointer"
  >
    <Plus size={16} />
    Aggiungi transazione
  </button>
)}
```

AFTER (unconditional, updated style, shorter label):
```jsx
<button
  onClick={onAddTransaction}
  aria-label="Aggiungi transazione"
  className="flex items-center gap-2 px-4 py-2 bg-brand-600 hover:bg-brand-700 text-white text-sm font-semibold rounded-lg transition-colors cursor-pointer"
>
  <Plus size={16} />
  Aggiungi
</button>
```

Key changes:
- Remove `{view === 'transactions' && (...)}` wrapper — button now renders unconditionally
- Change `bg-brand-500 hover:bg-brand-600` → `bg-brand-600 hover:bg-brand-700` (per CONTEXT.md Decision B)
- Shorten label: "Aggiungi transazione" → "Aggiungi" (fits header without overflow)
- Add `aria-label="Aggiungi transazione"` for accessibility (screen readers get full label)
  </action>
  <verify>
    <automated>grep -n "view === 'transactions'" src/components/layout/AppHeader.jsx; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - AppHeader.jsx does NOT contain `view === 'transactions'` condition around button
    - AppHeader.jsx contains `aria-label="Aggiungi transazione"`
    - AppHeader.jsx contains `bg-brand-600 hover:bg-brand-700`
    - AppHeader.jsx button label is `Aggiungi` (not `Aggiungi transazione`)
    - AppHeader.jsx button still has `onClick={onAddTransaction}`
    - AppHeader.jsx button has `cursor-pointer` class
  </acceptance_criteria>
  <done>AppHeader "Aggiungi" button now renders unconditionally on all views with updated styling and aria-label</done>
</task>

</tasks>

<verification>
**Component verification:**
1. AddTransactionModal.jsx exists with ModalShell wrapper
2. AppHeader.jsx no longer has view === 'transactions' guard
3. Button uses bg-brand-600 hover:bg-brand-700 (not 500/600)

**Build verification:**
```bash
npm run build
```
Exit 0 = imports resolve, no syntax errors.

**Note:** Full wiring in App.jsx (AnimatePresence wrapper for modal, showAddTransaction destructuring) is handled in Plan 03.
</verification>

<success_criteria>
- AddTransactionModal.jsx exists and uses ModalShell size="sm"
- Modal has 4 form fields: date, importo, descrizione, categoria
- Modal primary button disabled when description empty
- AppHeader shows "Aggiungi" button on dashboard, transactions, AND settings views
- AppHeader button has aria-label="Aggiungi transazione"
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-ux-polish/07-02-SUMMARY.md`
</output>

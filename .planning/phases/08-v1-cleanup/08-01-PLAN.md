---
phase: 08-v1-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/SettingsView.jsx
  - src/hooks/useFilters.js
  - src/hooks/useModals.js
  - src/App.jsx
  - src/views/DashboardView.jsx
  - src/components/modals/AddTransactionModal.jsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [UX-06, FOUND-11]
must_haves:
  truths:
    - "Both SettingsView buttons show pointer cursor on hover"
    - "useFilters returns only actively consumed state — no dashboardTypeFilter, expandedCategory, showCategoryPercentage"
    - "useModals returns only actively consumed state — no openDropdown"
    - "No selectedYear === null ternary guards or null-coalescing fallbacks exist in App.jsx or DashboardView.jsx"
    - "AddTransactionModal imports ModalShell via named barrel pattern"
    - "REQUIREMENTS.md FOUND-11 checkbox is [x] and traceability status is Complete"
  artifacts:
    - path: "src/views/SettingsView.jsx"
      provides: "cursor-pointer on both buttons"
      contains: "cursor-pointer"
    - path: "src/hooks/useFilters.js"
      provides: "Clean hook with no dead state"
    - path: "src/hooks/useModals.js"
      provides: "Clean hook with no dead state"
    - path: "src/App.jsx"
      provides: "No dead selectedYear null branches"
    - path: "src/views/DashboardView.jsx"
      provides: "Simplified period label without null guard"
    - path: "src/components/modals/AddTransactionModal.jsx"
      provides: "Named barrel import for ModalShell"
      contains: "import { ModalShell } from '../ui'"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate traceability for FOUND-04 and FOUND-11"
  key_links: []
---

<objective>
Close all 5 tech debt items from v1.0-MILESTONE-AUDIT.md in a single cleanup pass: cursor fix, dead state removal, dead code branch simplification, import normalization, and documentation closure.

Purpose: Complete the v1.0 milestone with zero outstanding gaps — 47/47 requirements satisfied and clean codebase.
Output: 6 cleaned source files + accurate REQUIREMENTS.md traceability.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-v1-cleanup/08-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code cleanup — cursor fix, dead state removal, dead branches, import normalization</name>
  <files>
    src/views/SettingsView.jsx,
    src/hooks/useFilters.js,
    src/hooks/useModals.js,
    src/App.jsx,
    src/views/DashboardView.jsx,
    src/components/modals/AddTransactionModal.jsx
  </files>
  <read_first>
    src/views/SettingsView.jsx,
    src/hooks/useFilters.js,
    src/hooks/useModals.js,
    src/App.jsx,
    src/views/DashboardView.jsx,
    src/components/modals/AddTransactionModal.jsx,
    src/components/ui/index.js
  </read_first>
  <action>
    **Before any edits:** Run `grep -rn "dashboardTypeFilter\|expandedCategory\|showCategoryPercentage\|openDropdown" src/ --include="*.jsx" --include="*.js"` to confirm these identifiers have zero external consumers outside their hook definition+return. Proceed only if all matches are confined to useFilters.js and useModals.js.

    **1. SettingsView.jsx — Add cursor-pointer to both buttons (UX-06 gap closure)**

    Line 19 className — change:
    `className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors"`
    to:
    `className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors cursor-pointer"`

    Line 31 className — change:
    `className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors"`
    to:
    `className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors cursor-pointer"`

    **2. useFilters.js — Remove 3 dead useState pairs**

    Delete these 3 useState declarations entirely:
    - `const [dashboardTypeFilter, setDashboardTypeFilter] = useState('all');`
    - `const [expandedCategory, setExpandedCategory] = useState(null);`
    - `const [showCategoryPercentage, setShowCategoryPercentage] = useState(false);`

    Delete these 3 lines from the return object:
    - `dashboardTypeFilter, setDashboardTypeFilter,`
    - `expandedCategory, setExpandedCategory,`
    - `showCategoryPercentage, setShowCategoryPercentage,`

    Update JSDoc comment to remove mention of these state pairs (or just keep the existing generic description — it doesn't enumerate individual states).

    **3. useModals.js — Remove 1 dead useState pair**

    Delete this useState declaration:
    - `const [openDropdown, setOpenDropdown] = useState(null);`

    Delete from return object:
    - `openDropdown, setOpenDropdown,`

    **4. App.jsx — Remove selectedYear === null dead code (7 locations)**

    Since `selectedYear` is always a number (initialized to `new Date().getFullYear()` in useFilters, never set to null via any UI path), simplify all null guards:

    Location 1 (stats useMemo, ~line 157-159) — change:
    ```
    let filtered = selectedYear !== null
      ? transactions.filter((t) => new Date(t.date).getFullYear() === selectedYear)
      : [...transactions];
    ```
    to:
    ```
    let filtered = transactions.filter((t) => new Date(t.date).getFullYear() === selectedYear);
    ```

    Location 2 (stats useMemo, ~line 217-219) — change:
    ```
    let monthlyFiltered = selectedYear !== null
      ? transactions.filter((t) => new Date(t.date).getFullYear() === selectedYear)
      : [...transactions];
    ```
    to:
    ```
    let monthlyFiltered = transactions.filter((t) => new Date(t.date).getFullYear() === selectedYear);
    ```

    Location 3 (previous period guard, ~line 320) — change:
    ```
    if (selectedMonth !== null && selectedYear !== null) {
    ```
    to:
    ```
    if (selectedMonth !== null) {
    ```

    Location 4 (availableMonths useMemo, ~line 357-359) — change:
    ```
    const base = selectedYear !== null
      ? transactions.filter(t => new Date(t.date).getFullYear() === selectedYear)
      : transactions;
    ```
    to:
    ```
    const base = transactions.filter(t => new Date(t.date).getFullYear() === selectedYear);
    ```

    Location 5 (handlePrevYear, ~line 365) — change:
    `setSelectedYear(y => (y ?? new Date().getFullYear()) - 1);`
    to:
    `setSelectedYear(y => y - 1);`

    Location 6 (handleNextYear, ~line 370) — change:
    `setSelectedYear(y => (y ?? new Date().getFullYear()) + 1);`
    to:
    `setSelectedYear(y => y + 1);`

    Location 7 (handleSelectMonth, ~line 374-377) — change:
    ```
    const handleSelectMonth = useCallback((month) => {
      if (selectedYear === null) setSelectedYear(new Date().getFullYear());
      setSelectedMonth(prev => (prev === month ? null : month));
    }, [selectedYear, setSelectedYear, setSelectedMonth]);
    ```
    to:
    ```
    const handleSelectMonth = useCallback((month) => {
      setSelectedMonth(prev => (prev === month ? null : month));
    }, [setSelectedMonth]);
    ```

    **5. DashboardView.jsx — Simplify period label + update JSDoc**

    JSDoc (~line 16) — change:
    `selectedYear: number|null,`
    to:
    `selectedYear: number,`

    Period label (~lines 96-100) — change:
    ```
    {selectedMonth !== null && selectedYear !== null
      ? `${MONTHS_IT[selectedMonth]} ${selectedYear}`
      : selectedYear !== null
        ? `Anno ${selectedYear}`
        : 'Tutti gli anni'}
    ```
    to:
    ```
    {selectedMonth !== null
      ? `${MONTHS_IT[selectedMonth]} ${selectedYear}`
      : `Anno ${selectedYear}`}
    ```

    **6. AddTransactionModal.jsx — Normalize ModalShell import**

    Line 3 — change:
    `import ModalShell from '../ui/ModalShell';`
    to:
    `import { ModalShell } from '../ui';`
  </action>
  <verify>
    <automated>cd D:\Generale\budget-tracker &amp;&amp; npx vite build 2>&amp;1 | tail -5 &amp;&amp; npx eslint src/views/SettingsView.jsx src/hooks/useFilters.js src/hooks/useModals.js src/App.jsx src/views/DashboardView.jsx src/components/modals/AddTransactionModal.jsx 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - `src/views/SettingsView.jsx` contains `cursor-pointer` exactly 2 times (one per button)
    - `src/hooks/useFilters.js` does NOT contain `dashboardTypeFilter`
    - `src/hooks/useFilters.js` does NOT contain `expandedCategory`
    - `src/hooks/useFilters.js` does NOT contain `showCategoryPercentage`
    - `src/hooks/useModals.js` does NOT contain `openDropdown`
    - `src/App.jsx` does NOT contain `selectedYear !== null`
    - `src/App.jsx` does NOT contain `selectedYear === null`
    - `src/App.jsx` does NOT contain `y ?? new Date`
    - `src/views/DashboardView.jsx` does NOT contain `Tutti gli anni`
    - `src/views/DashboardView.jsx` contains `selectedYear: number,` (not `number|null`)
    - `src/components/modals/AddTransactionModal.jsx` contains `import { ModalShell } from '../ui'`
    - `src/components/modals/AddTransactionModal.jsx` does NOT contain `from '../ui/ModalShell'`
    - `npx vite build` exits 0
    - `npx eslint` on all 6 files exits 0 (or only pre-existing warnings)
  </acceptance_criteria>
  <done>All 6 source files cleaned: SettingsView has cursor-pointer on both buttons, useFilters has 3 fewer state pairs, useModals has 1 fewer state pair, App.jsx has no selectedYear null guards, DashboardView period label simplified, AddTransactionModal uses barrel import. Build and lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation closure — FOUND-11 checkbox + traceability table accuracy</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
    **1. FOUND-11 checkbox (line 20)** — change:
    `- [ ] **FOUND-11**: Backup localStorage automatico prima di ogni refactor step (recovery point) *(to be closed in Phase 8 — dev-process req, Electron runtime backup present)*`
    to:
    `- [x] **FOUND-11**: Backup localStorage automatico prima di ogni refactor step (recovery point) *(closed Phase 8 — dev-process requirement; Electron runtime backup via backupDataRef + electronAPI.onRequestBackupData in useTransactionData.js satisfies data-safety intent; all 7 refactoring phases complete)*`

    **2. FOUND-11 traceability table row (line 115)** — change:
    `| FOUND-11 | Phase 8 (gap closure) | Pending |`
    to:
    `| FOUND-11 | Phase 8 | Complete |`

    **3. FOUND-04 traceability verification** — Read line 108. It currently shows `| FOUND-04 | Phase 1 | Complete |`. Confirm this is correct (the audit flagged it as "Pending" but the file already shows "Complete"). If for some reason it still says "Pending", change it to "Complete". No edit needed if already correct.

    **4. Coverage count** — Line 153-156 currently shows `47 total, 47 mapped, 0 unmapped`. This remains correct. No change.
  </action>
  <verify>
    <automated>cd D:\Generale\budget-tracker &amp;&amp; grep -n "FOUND-11" .planning/REQUIREMENTS.md &amp;&amp; grep -n "FOUND-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` line containing `FOUND-11` checkbox has `[x]` (not `[ ]`)
    - `.planning/REQUIREMENTS.md` line containing `FOUND-11` in traceability table shows `| Complete |` (not `| Pending |`)
    - `.planning/REQUIREMENTS.md` line containing `FOUND-04` in traceability table shows `| Complete |`
    - `.planning/REQUIREMENTS.md` contains the text `backupDataRef` in the FOUND-11 inline annotation
    - No other lines in REQUIREMENTS.md were changed
  </acceptance_criteria>
  <done>REQUIREMENTS.md traceability is fully accurate: FOUND-11 marked [x] Complete with inline rationale, FOUND-04 confirmed Complete. 47/47 requirements satisfied.</done>
</task>

</tasks>

<verification>
After both tasks complete, run full verification:

```bash
# Build must pass
npx vite build

# Lint all modified source files
npx eslint src/views/SettingsView.jsx src/hooks/useFilters.js src/hooks/useModals.js src/App.jsx src/views/DashboardView.jsx src/components/modals/AddTransactionModal.jsx

# Confirm no dead state identifiers leaked
grep -rn "dashboardTypeFilter\|expandedCategory\|showCategoryPercentage" src/ --include="*.jsx" --include="*.js"
# Expected: 0 matches

grep -rn "openDropdown" src/ --include="*.jsx" --include="*.js"
# Expected: 0 matches

# Confirm no selectedYear null patterns remain
grep -rn "selectedYear !== null\|selectedYear === null\|y ?? new Date" src/ --include="*.jsx" --include="*.js"
# Expected: 0 matches

# Confirm cursor-pointer on SettingsView buttons
grep -c "cursor-pointer" src/views/SettingsView.jsx
# Expected: 2

# Confirm barrel import pattern
grep "from '../ui'" src/components/modals/AddTransactionModal.jsx
# Expected: import { ModalShell } from '../ui';

# Confirm FOUND-11 closure
grep "FOUND-11" .planning/REQUIREMENTS.md
# Expected: [x] checkbox + Complete in traceability
```
</verification>

<success_criteria>
1. `npx vite build` exits 0
2. `npx eslint` on all 6 source files exits 0 (pre-existing warnings acceptable)
3. Zero grep matches for `dashboardTypeFilter`, `expandedCategory`, `showCategoryPercentage`, `openDropdown` in src/
4. Zero grep matches for `selectedYear !== null`, `selectedYear === null`, `y ?? new Date` in src/
5. `cursor-pointer` appears exactly 2 times in SettingsView.jsx
6. AddTransactionModal.jsx contains `import { ModalShell } from '../ui'`
7. REQUIREMENTS.md FOUND-11 shows `[x]` and `Complete`
8. All 5 audit success criteria met: cursor fix, dead state clean, dead branches gone, barrel import, docs accurate
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-cleanup/08-01-SUMMARY.md`
</output>

---
phase: 06-modals-redesign
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - src/App.jsx
autonomous: true
requirements: [MOD-01, MOD-02]

must_haves:
  truths:
    - "All 7 modals are wrapped with AnimatePresence in App.jsx"
    - "AnimatePresence is imported from framer-motion in App.jsx"
    - "Each modal has a unique key prop for AnimatePresence tracking"
    - "SettingsView receives onShowCategoryManager and onShowSyncSettings props"
    - "Exit animations play when modals close (enabled by AnimatePresence)"
  artifacts:
    - path: "src/App.jsx"
      provides: "AnimatePresence wrappers for all modals"
      contains: "import { AnimatePresence } from 'framer-motion'"
  key_links:
    - from: "src/App.jsx"
      to: "framer-motion"
      via: "AnimatePresence wrapper"
      pattern: "<AnimatePresence>"
    - from: "src/App.jsx"
      to: "src/views/SettingsView.jsx"
      via: "onShowCategoryManager and onShowSyncSettings props"
      pattern: "onShowCategoryManager.*setShowCategoryManager"
---

<objective>
Wire AnimatePresence wrappers in App.jsx for exit animations + pass SettingsView props

Purpose: Enable modal exit animations by wrapping conditional modal renders with AnimatePresence. Connect SettingsView buttons to modal state.

Output:
- App.jsx imports AnimatePresence from framer-motion
- All 7 modal conditionals wrapped with AnimatePresence
- SettingsView receives modal trigger props
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-modals-redesign/06-CONTEXT.md
@.planning/phases/06-modals-redesign/06-RESEARCH.md
@.planning/phases/06-modals-redesign/06-02-SUMMARY.md
@.planning/phases/06-modals-redesign/06-03-SUMMARY.md

<interfaces>
<!-- AnimatePresence usage pattern -->
```jsx
import { AnimatePresence } from 'framer-motion';

// Wrap each conditional modal render:
<AnimatePresence>
  {showCategoryManager && (
    <CategoryManager key="category-manager" ... />
  )}
</AnimatePresence>
```

<!-- All 7 modals to wrap: -->
1. CategoryManager — condition: showCategoryManager
2. SyncSettings — condition: showSyncSettings
3. PayPalEnrichWizard — condition: paypalData
4. ConfirmModal — condition: confirmDelete
5. ImportWizard — condition: wizardData
6. ConflictResolver — condition: importConflicts
7. CategoryConflictResolver — condition: categoryConflicts

<!-- SettingsView props to add: -->
```jsx
<SettingsView
  onShowCategoryManager={() => setShowCategoryManager(true)}
  onShowSyncSettings={() => setShowSyncSettings(true)}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AnimatePresence import to App.jsx</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (current imports section)
  </read_first>
  <action>
Add AnimatePresence to the framer-motion import in App.jsx.

**Find existing import (if any):** Search for `import.*framer-motion` in App.jsx.

**If no framer-motion import exists:** Add this import near the top with other library imports:
```jsx
import { AnimatePresence } from 'framer-motion';
```

**If framer-motion is already imported:** Add AnimatePresence to the existing import:
```jsx
import { AnimatePresence } from 'framer-motion';
// or extend existing: import { motion, AnimatePresence } from 'framer-motion';
```

This enables exit animations for modals by delaying unmount until exit animation completes.
  </action>
  <verify>
    <automated>Select-String -Path "D:\Generale\budget-tracker\src\App.jsx" -Pattern "AnimatePresence.*framer-motion" | ForEach-Object { $_.Line }</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx contains `import { AnimatePresence } from 'framer-motion'` or equivalent
    - Import is syntactically correct (build passes)
  </acceptance_criteria>
  <done>AnimatePresence imported from framer-motion in App.jsx</done>
</task>

<task type="auto">
  <name>Task 2: Wrap all modals with AnimatePresence</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (modal render section — search for CategoryManager, SyncSettings, etc.)
    - .planning/phases/06-modals-redesign/06-RESEARCH.md (Pattern 2: AnimatePresence in App.jsx)
  </read_first>
  <action>
Wrap each of the 7 modal conditional renders with `<AnimatePresence>`:

**1. CategoryManager:**
```jsx
{/* Category Manager Modal */}
<AnimatePresence>
  {showCategoryManager && (
    <CategoryManager
      key="category-manager"
      categories={categories}
      categoriesChanged={categoriesChanged}
      onAddCategory={addCategory}
      onDeleteCategory={deleteCategory}
      onAddKeyword={addKeyword}
      onRemoveKeyword={removeKeyword}
      onRecategorize={() => recategorizeAll(transactions, categoryResolutions, setTransactions)}
      onClose={() => setShowCategoryManager(false)}
    />
  )}
</AnimatePresence>
```

**2. SyncSettings:**
```jsx
{/* Sync Settings Modal */}
<AnimatePresence>
  {showSyncSettings && (
    <SyncSettings
      key="sync-settings"
      isAuthenticated={googleDrive.isAuthenticated}
      hasDrivePermission={googleDrive.hasDrivePermission}
      isLoading={googleDrive.isLoading}
      userInfo={googleDrive.userInfo}
      backupInfo={googleDrive.backupInfo}
      lastSyncTime={googleDrive.lastSyncTime}
      syncStatus={googleDrive.syncStatus}
      error={googleDrive.error}
      onSignIn={/* existing */}
      onCancelSignIn={/* existing */}
      onSignOut={/* existing */}
      onUpload={/* existing */}
      onDownload={/* existing */}
      onDelete={/* existing */}
      onClose={() => setShowSyncSettings(false)}
    />
  )}
</AnimatePresence>
```

**3. PayPalEnrichWizard:**
```jsx
{/* PayPal Enrich Wizard */}
<AnimatePresence>
  {paypalData && (
    <PayPalEnrichWizard
      key="paypal-enrich-wizard"
      transactions={transactions}
      paypalData={paypalData}
      onConfirm={applyPayPalEnrichment}
      onCancel={() => setPaypalData(null)}
    />
  )}
</AnimatePresence>
```

**4. ConfirmModal:**
```jsx
{/* Confirm Modal */}
<AnimatePresence>
  {confirmDelete && (
    <ConfirmModal
      key="confirm-modal"
      title={/* existing */}
      message={/* existing */}
      onConfirm={/* existing */}
      onCancel={() => setConfirmDelete(null)}
    />
  )}
</AnimatePresence>
```

**5. ImportWizard:**
```jsx
{/* Import Wizard Modal */}
<AnimatePresence>
  {wizardData && (
    <ImportWizard
      key="import-wizard"
      columns={wizardData.columns}
      sampleData={wizardData.sampleData}
      onConfirm={handleWizardConfirm}
      onCancel={() => setWizardData(null)}
    />
  )}
</AnimatePresence>
```

**6. ConflictResolver:**
```jsx
{/* Conflict Resolver Modal */}
<AnimatePresence>
  {importConflicts && (
    <ConflictResolver
      key="conflict-resolver"
      conflicts={importConflicts.conflicts}
      onResolve={handleConflictResolve}
      onCancel={() => setImportConflicts(null)}
    />
  )}
</AnimatePresence>
```

**7. CategoryConflictResolver:**
```jsx
{/* Category Conflict Resolver Modal */}
<AnimatePresence>
  {categoryConflicts && (
    <CategoryConflictResolver
      key="category-conflict-resolver"
      conflicts={categoryConflicts}
      onConfirm={confirmCategoryConflicts}
      onClose={() => setCategoryConflicts(null)}
    />
  )}
</AnimatePresence>
```

**Key points:**
- Each modal gets a unique `key` prop for AnimatePresence tracking
- Preserve ALL existing props on each modal
- Only add the AnimatePresence wrapper and key prop
  </action>
  <verify>
    <automated>Select-String -Path "D:\Generale\budget-tracker\src\App.jsx" -Pattern "<AnimatePresence>" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx contains exactly 7 `<AnimatePresence>` wrapper occurrences
    - Each modal has `key="..."` prop
    - CategoryManager has `key="category-manager"`
    - SyncSettings has `key="sync-settings"`
    - PayPalEnrichWizard has `key="paypal-enrich-wizard"`
    - ConfirmModal has `key="confirm-modal"`
    - ImportWizard has `key="import-wizard"`
    - ConflictResolver has `key="conflict-resolver"`
    - CategoryConflictResolver has `key="category-conflict-resolver"`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 7 modals wrapped with AnimatePresence for exit animation support</done>
</task>

<task type="auto">
  <name>Task 3: Pass modal trigger props to SettingsView</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (SettingsView render — search for `<SettingsView`)
    - src/views/SettingsView.jsx (to confirm prop names)
  </read_first>
  <action>
Find the SettingsView render in App.jsx (currently `{view === 'settings' && <SettingsView />}`) and add the modal trigger props:

**Before:**
```jsx
{/* Settings View */}
{view === 'settings' && <SettingsView />}
```

**After:**
```jsx
{/* Settings View */}
{view === 'settings' && (
  <SettingsView
    onShowCategoryManager={() => setShowCategoryManager(true)}
    onShowSyncSettings={() => setShowSyncSettings(true)}
  />
)}
```

This connects the SettingsView buttons (created in Plan 03) to the modal state in App.jsx.
  </action>
  <verify>
    <automated>Select-String -Path "D:\Generale\budget-tracker\src\App.jsx" -Pattern "SettingsView.*onShowCategoryManager" | ForEach-Object { $_.Line }</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx contains `<SettingsView` with props
    - App.jsx contains `onShowCategoryManager={() => setShowCategoryManager(true)}`
    - App.jsx contains `onShowSyncSettings={() => setShowSyncSettings(true)}`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SettingsView receives modal trigger props, buttons are functional</done>
</task>

</tasks>

<verification>
After all tasks:

1. **Build check:** `npm run build` — must exit 0
2. **Lint check:** `npm run lint` — must exit 0 (warnings OK)
3. **AnimatePresence count:** `Select-String -Path src/App.jsx -Pattern "<AnimatePresence>"` returns 7 matches
4. **SettingsView props:** App.jsx passes onShowCategoryManager and onShowSyncSettings to SettingsView

Phase 6 implementation complete. All 7 modals have:
- ModalShell wrapper (from Plans 02-03)
- AnimatePresence for exit animations (this plan)
- Consistent Tailwind styling
- Radix a11y (focus trap, ESC, backdrop click)
</verification>

<success_criteria>
- AnimatePresence imported from framer-motion
- All 7 modals wrapped with AnimatePresence (7 wrappers total)
- Each modal has unique key prop
- SettingsView receives onShowCategoryManager and onShowSyncSettings props
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-modals-redesign/06-04-SUMMARY.md`
</output>

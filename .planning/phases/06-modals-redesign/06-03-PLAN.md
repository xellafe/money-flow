---
phase: 06-modals-redesign
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/modals/ImportWizard.jsx
  - src/components/modals/CategoryManager.jsx
  - src/views/SettingsView.jsx
autonomous: true
requirements: [MOD-05, MOD-06]

must_haves:
  truths:
    - "ImportWizard uses ModalShell wrapper with size='lg'"
    - "CategoryManager uses ModalShell wrapper with size='lg'"
    - "SettingsView has two buttons that trigger CategoryManager and SyncSettings modals"
  artifacts:
    - path: "src/components/modals/ImportWizard.jsx"
      provides: "Migrated import wizard modal"
      contains: "import { ModalShell } from '../ui'"
    - path: "src/components/modals/CategoryManager.jsx"
      provides: "Migrated category manager modal"
      contains: "import { ModalShell } from '../ui'"
    - path: "src/views/SettingsView.jsx"
      provides: "Settings view with modal trigger buttons"
      contains: "onShowCategoryManager"
  key_links:
    - from: "src/views/SettingsView.jsx"
      to: "App.jsx"
      via: "onShowCategoryManager and onShowSyncSettings props"
      pattern: "onShowCategoryManager|onShowSyncSettings"
---

<objective>
Migrate ImportWizard and CategoryManager to ModalShell, add SettingsView buttons

Purpose: Migrate two medium-complexity modals and add functional buttons to SettingsView to trigger CategoryManager and SyncSettings.

Output:
- 2 modals refactored with ModalShell + Tailwind styles
- SettingsView extended with two modal trigger buttons
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-modals-redesign/06-CONTEXT.md
@.planning/phases/06-modals-redesign/06-UI-SPEC.md
@.planning/phases/06-modals-redesign/06-RESEARCH.md
@.planning/phases/06-modals-redesign/06-01-SUMMARY.md

<interfaces>
<!-- ModalShell API -->
```jsx
import { ModalShell } from '../ui';
<ModalShell title="Title" onClose={fn} size="lg">{children}</ModalShell>
```

<!-- Button styles -->
Primary: `bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-medium transition-colors`
Secondary: `bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors`
Destructive: `bg-red-600 hover:bg-red-700 text-white rounded-lg px-4 py-2 text-sm font-medium transition-colors`

<!-- PayPalEnrichWizard step animation (from 06-UI-SPEC.md) -->
Forward: outgoing x:0→-30 opacity→0, incoming x:30→0 opacity→1, 200ms ease-out
Back: reverse direction

<!-- SettingsView button copy -->
- "Gestione Categorie" → triggers showCategoryManager
- "Sincronizzazione Drive" → triggers showSyncSettings

<!-- Form styling patterns -->
Labels: `text-sm font-medium text-gray-700 mb-1`
Inputs: `w-full px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-brand-500`
Selects: same as inputs
Form groups: `space-y-4`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ImportWizard to ModalShell</name>
  <files>src/components/modals/ImportWizard.jsx</files>
  <read_first>
    - src/components/modals/ImportWizard.jsx (current implementation — 165 lines)
    - src/components/ui/ModalShell.jsx (API)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (form styling, copywriting)
  </read_first>
  <action>
Refactor ImportWizard.jsx:

1. **Add import:** `import { ModalShell } from '../ui';`

2. **Replace outer wrapper:** Remove `<div className="modal-overlay">...<div className="modal modal-large">` and wrap with:
   ```jsx
   <ModalShell title="Importa transazioni" onClose={onCancel} size="lg">
   ```

3. **Remove:** Old `<h3 className="modal-title">` and Settings icon (ModalShell provides title)

4. **Style description paragraph:**
   ```jsx
   <p className="text-gray-600 mb-6">
     Non ho riconosciuto il formato di questo file. Configura la mappatura delle colonne.
   </p>
   ```

5. **Style form with space-y-4:**
   ```jsx
   <div className="space-y-4">
   ```

6. **Style each form field group:**
   ```jsx
   <div>
     <label className="block text-sm font-medium text-gray-700 mb-1">
       Nome profilo (per riutilizzarlo in futuro)
     </label>
     <input 
       type="text" 
       value={profileName} 
       onChange={e => setProfileName(e.target.value)}
       placeholder="Es: La mia banca"
       className="w-full px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-brand-500"
     />
   </div>
   ```

7. **Style select dropdowns:**
   ```jsx
   <select
     value={dateColumn}
     onChange={e => setDateColumn(e.target.value)}
     className="w-full px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-brand-500 bg-white"
   >
     <option value="">Seleziona colonna</option>
     {columns.map(col => <option key={col} value={col}>{col}</option>)}
   </select>
   ```

8. **Style radio button group (amount type):**
   ```jsx
   <div className="flex gap-4">
     <label className="flex items-center gap-2 cursor-pointer">
       <input
         type="radio"
         checked={amountType === 'single'}
         onChange={() => setAmountType('single')}
         className="accent-brand-600"
       />
       <span className="text-sm text-gray-700">Colonna singola</span>
     </label>
     <label className="flex items-center gap-2 cursor-pointer">
       <input
         type="radio"
         checked={amountType === 'separate'}
         onChange={() => setAmountType('separate')}
         className="accent-brand-600"
       />
       <span className="text-sm text-gray-700">Entrate/Uscite separate</span>
     </label>
   </div>
   ```

9. **Style footer buttons:**
   ```jsx
   <div className="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-200">
     <button
       className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors"
       onClick={onCancel}
     >
       Annulla
     </button>
     <button
       className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-medium transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-2"
       onClick={handleConfirm}
       disabled={!isValid}
     >
       <Check size={16} /> Importa file
     </button>
   </div>
   ```

10. **Remove wizard-form, wizard-field class names** — replace with Tailwind utilities
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { ModalShell } from '../ui'`
    - File contains `<ModalShell title="Importa transazioni" onClose={onCancel} size="lg">`
    - File does NOT contain `className="modal-overlay"`
    - File does NOT contain `className="wizard-form"`
    - File does NOT contain `className="wizard-field"`
    - File contains `focus:ring-brand-500` (form focus styling)
    - File contains `disabled:opacity-50` (disabled button state)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ImportWizard migrated to ModalShell with Tailwind form styling</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CategoryManager to ModalShell</name>
  <files>src/components/modals/CategoryManager.jsx</files>
  <read_first>
    - src/components/modals/CategoryManager.jsx (current implementation — 167 lines)
    - src/components/ui/ModalShell.jsx (API)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (styling)
    - .planning/phases/06-modals-redesign/06-RESEARCH.md (Pitfall 6 — internal styling classes)
  </read_first>
  <action>
Refactor CategoryManager.jsx:

1. **Add import:** `import { ModalShell } from '../ui';`

2. **Replace outer wrapper:** Remove modal-overlay/modal wrapper, use:
   ```jsx
   <ModalShell title="Gestione Categorie" onClose={onClose} size="lg">
   ```

3. **Style category grid container:**
   ```jsx
   <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
   ```

4. **Style each category card:**
   ```jsx
   <div className="p-4 bg-gray-50 rounded-lg border border-gray-200">
     <div className="flex items-center justify-between mb-3">
       <h4 className="font-medium text-gray-800">{category.name}</h4>
       <button
         onClick={() => onDeleteCategory(category.name)}
         className="p-1 text-gray-400 hover:text-red-600 hover:bg-red-50 rounded transition-colors"
         title="Elimina categoria"
       >
         <Trash2 size={16} />
       </button>
     </div>
     
     {/* Keywords */}
     <div className="flex flex-wrap gap-2 mb-3">
       {category.keywords.map(keyword => (
         <span key={keyword} className="inline-flex items-center gap-1 px-2 py-1 bg-white border border-gray-200 rounded text-sm text-gray-700">
           {keyword}
           <button
             onClick={() => onRemoveKeyword(category.name, keyword)}
             className="text-gray-400 hover:text-red-500"
           >
             <X size={12} />
           </button>
         </span>
       ))}
     </div>
     
     {/* Add keyword input */}
     <div className="flex gap-2">
       <input
         type="text"
         placeholder="Nuova keyword"
         className="flex-1 px-2 py-1 text-sm border border-gray-200 rounded focus:outline-none focus:ring-1 focus:ring-brand-500"
         onKeyDown={(e) => {
           if (e.key === 'Enter' && e.target.value.trim()) {
             onAddKeyword(category.name, e.target.value.trim());
             e.target.value = '';
           }
         }}
       />
     </div>
   </div>
   ```

5. **Style add category section:**
   ```jsx
   <div className="flex gap-3 mb-6">
     <input
       type="text"
       value={newCategoryName}
       onChange={e => setNewCategoryName(e.target.value)}
       placeholder="Nome nuova categoria"
       className="flex-1 px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500"
     />
     <button
       onClick={handleAddCategory}
       disabled={!newCategoryName.trim()}
       className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-medium transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-2"
     >
       <Plus size={16} /> Aggiungi categoria
     </button>
   </div>
   ```

6. **Style recategorize button (if categoriesChanged):**
   ```jsx
   {categoriesChanged && (
     <div className="p-4 bg-amber-50 border border-amber-200 rounded-lg mb-6">
       <p className="text-amber-800 text-sm mb-2">
         Hai modificato le categorie. Vuoi ricategorizzare le transazioni esistenti?
       </p>
       <button
         onClick={onRecategorize}
         className="bg-amber-600 hover:bg-amber-700 text-white rounded-lg px-4 py-2 text-sm font-medium transition-colors"
       >
         Ricategorizza transazioni
       </button>
     </div>
   )}
   ```

7. **Remove all old class names:** `.categories-grid`, `.category-card`, `.keyword-tag`, `.btn-delete`, `.search-input`
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { ModalShell } from '../ui'`
    - File contains `<ModalShell title="Gestione Categorie" onClose={onClose} size="lg">`
    - File does NOT contain `className="modal-overlay"`
    - File does NOT contain `className="categories-grid"`
    - File does NOT contain `className="category-card"`
    - File contains `grid grid-cols-1 md:grid-cols-2 gap-4`
    - File contains `focus:ring-brand-500`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>CategoryManager migrated to ModalShell with grid layout and Tailwind-styled category cards</done>
</task>

<task type="auto">
  <name>Task 3: Extend SettingsView with modal trigger buttons</name>
  <files>src/views/SettingsView.jsx</files>
  <read_first>
    - src/views/SettingsView.jsx (current placeholder — 10 lines)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (SettingsView buttons section)
    - .planning/phases/06-modals-redesign/06-RESEARCH.md (Pattern 4: SettingsView buttons)
  </read_first>
  <action>
Replace SettingsView.jsx with:

```jsx
/**
 * Settings view with modal trigger buttons for CategoryManager and SyncSettings
 * @param {Object} props
 * @param {Function} props.onShowCategoryManager - Opens CategoryManager modal
 * @param {Function} props.onShowSyncSettings - Opens SyncSettings modal
 */
export function SettingsView({ onShowCategoryManager, onShowSyncSettings }) {
  return (
    <div className="p-8">
      <h2 className="text-2xl font-semibold text-gray-800 mb-6">Impostazioni</h2>

      <div className="space-y-6">
        <section>
          <h3 className="text-sm font-medium text-gray-500 uppercase tracking-wider mb-3">
            Categorie
          </h3>
          <button
            onClick={onShowCategoryManager}
            className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors"
          >
            Gestione Categorie
          </button>
        </section>

        <section>
          <h3 className="text-sm font-medium text-gray-500 uppercase tracking-wider mb-3">
            Sincronizzazione
          </h3>
          <button
            onClick={onShowSyncSettings}
            className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-medium transition-colors"
          >
            Sincronizzazione Drive
          </button>
        </section>
      </div>
    </div>
  );
}
```

This adds:
- `onShowCategoryManager` prop — triggers CategoryManager modal
- `onShowSyncSettings` prop — triggers SyncSettings modal
- Two sections with descriptive headings
- Consistent secondary button styling (font-medium per CONTEXT.md)
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `onShowCategoryManager` in props
    - File contains `onShowSyncSettings` in props
    - File contains `onClick={onShowCategoryManager}`
    - File contains `onClick={onShowSyncSettings}`
    - File contains "Gestione Categorie" button text
    - File contains "Sincronizzazione Drive" button text
    - File contains `bg-gray-100 hover:bg-gray-200` (secondary button style)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SettingsView extended with two modal trigger buttons (CategoryManager, SyncSettings)</done>
</task>

</tasks>

<verification>
After all tasks:

1. **Build check:** `npm run build` — must exit 0
2. **Lint check:** `npm run lint` — must exit 0 (warnings OK)
3. **Visual structure check:** ImportWizard and CategoryManager:
   - Import ModalShell from '../ui'
   - No .modal-overlay or .modal class names
   - Consistent button styling with Tailwind utilities (font-medium)
4. **SettingsView:** Has onShowCategoryManager and onShowSyncSettings props

Wave 2 modals (part 1) complete. Continue with Plan 04 (SyncSettings, PayPalEnrichWizard).
</verification>

<success_criteria>
- ImportWizard uses ModalShell with size="lg", Tailwind form styling
- CategoryManager uses ModalShell with size="lg", grid layout, keyword management
- SettingsView has two functional modal trigger buttons
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-modals-redesign/06-03-SUMMARY.md`
</output>

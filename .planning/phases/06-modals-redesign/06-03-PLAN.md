---
phase: 06-modals-redesign
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/components/modals/ImportWizard.jsx
  - src/components/modals/CategoryManager.jsx
  - src/components/modals/SyncSettings.jsx
  - src/components/modals/PayPalEnrichWizard.jsx
  - src/views/SettingsView.jsx
autonomous: true
requirements: [MOD-05, MOD-06, MOD-07]

must_haves:
  truths:
    - "ImportWizard uses ModalShell wrapper with size='lg'"
    - "CategoryManager uses ModalShell wrapper with size='lg'"
    - "SyncSettings uses ModalShell wrapper with size='lg'"
    - "PayPalEnrichWizard uses ModalShell wrapper with size='lg' and has internal step slide animations"
    - "SettingsView has two buttons that trigger CategoryManager and SyncSettings modals"
    - "Long-content modals scroll internally without breaking layout (MOD-07)"
  artifacts:
    - path: "src/components/modals/ImportWizard.jsx"
      provides: "Migrated import wizard modal"
      contains: "import { ModalShell } from '../ui'"
    - path: "src/components/modals/CategoryManager.jsx"
      provides: "Migrated category manager modal"
      contains: "import { ModalShell } from '../ui'"
    - path: "src/components/modals/SyncSettings.jsx"
      provides: "Migrated sync settings modal"
      contains: "import { ModalShell } from '../ui'"
    - path: "src/components/modals/PayPalEnrichWizard.jsx"
      provides: "Migrated PayPal enrich wizard with step animations"
      contains: "import { ModalShell } from '../ui'"
    - path: "src/views/SettingsView.jsx"
      provides: "Settings view with modal trigger buttons"
      contains: "onShowCategoryManager"
  key_links:
    - from: "src/views/SettingsView.jsx"
      to: "App.jsx"
      via: "onShowCategoryManager and onShowSyncSettings props"
      pattern: "onShowCategoryManager|onShowSyncSettings"
---

<objective>
Migrate complex modals (ImportWizard, CategoryManager, SyncSettings, PayPalEnrichWizard) and add SettingsView buttons

Purpose: Complete modal migration by handling the larger, more complex modals. Add functional buttons to SettingsView to trigger CategoryManager and SyncSettings.

Output:
- 4 complex modals refactored with ModalShell + Tailwind styles
- SettingsView extended with two modal trigger buttons
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-modals-redesign/06-CONTEXT.md
@.planning/phases/06-modals-redesign/06-UI-SPEC.md
@.planning/phases/06-modals-redesign/06-RESEARCH.md
@.planning/phases/06-modals-redesign/06-01-SUMMARY.md

<interfaces>
<!-- ModalShell API -->
```jsx
import { ModalShell } from '../ui';
<ModalShell title="Title" onClose={fn} size="lg">{children}</ModalShell>
```

<!-- Button styles -->
Primary: `bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors`
Secondary: `bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors`
Destructive: `bg-red-600 hover:bg-red-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors`

<!-- PayPalEnrichWizard step animation (from 06-UI-SPEC.md) -->
Forward: outgoing x:0→-30 opacity→0, incoming x:30→0 opacity→1, 200ms ease-out
Back: reverse direction

<!-- SettingsView button copy -->
- "Gestione Categorie" → triggers showCategoryManager
- "Sincronizzazione Drive" → triggers showSyncSettings

<!-- Form styling patterns -->
Labels: `text-sm font-medium text-gray-700 mb-1`
Inputs: `w-full px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-brand-500`
Selects: same as inputs
Form groups: `space-y-4`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ImportWizard to ModalShell</name>
  <files>src/components/modals/ImportWizard.jsx</files>
  <read_first>
    - src/components/modals/ImportWizard.jsx (current implementation — 165 lines)
    - src/components/ui/ModalShell.jsx (API)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (form styling, copywriting)
  </read_first>
  <action>
Refactor ImportWizard.jsx:

1. **Add import:** `import { ModalShell } from '../ui';`

2. **Replace outer wrapper:** Remove `<div className="modal-overlay">...<div className="modal modal-large">` and wrap with:
   ```jsx
   <ModalShell title="Importa transazioni" onClose={onCancel} size="lg">
   ```

3. **Remove:** Old `<h3 className="modal-title">` and Settings icon (ModalShell provides title)

4. **Style description paragraph:**
   ```jsx
   <p className="text-gray-600 mb-6">
     Non ho riconosciuto il formato di questo file. Configura la mappatura delle colonne.
   </p>
   ```

5. **Style form with space-y-4:**
   ```jsx
   <div className="space-y-4">
   ```

6. **Style each form field group:**
   ```jsx
   <div>
     <label className="block text-sm font-medium text-gray-700 mb-1">
       Nome profilo (per riutilizzarlo in futuro)
     </label>
     <input 
       type="text" 
       value={profileName} 
       onChange={e => setProfileName(e.target.value)}
       placeholder="Es: La mia banca"
       className="w-full px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-brand-500"
     />
   </div>
   ```

7. **Style select dropdowns:**
   ```jsx
   <select
     value={dateColumn}
     onChange={e => setDateColumn(e.target.value)}
     className="w-full px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-brand-500 bg-white"
   >
     <option value="">Seleziona colonna</option>
     {columns.map(col => <option key={col} value={col}>{col}</option>)}
   </select>
   ```

8. **Style radio button group (amount type):**
   ```jsx
   <div className="flex gap-4">
     <label className="flex items-center gap-2 cursor-pointer">
       <input
         type="radio"
         checked={amountType === 'single'}
         onChange={() => setAmountType('single')}
         className="accent-brand-600"
       />
       <span className="text-sm text-gray-700">Colonna singola</span>
     </label>
     <label className="flex items-center gap-2 cursor-pointer">
       <input
         type="radio"
         checked={amountType === 'separate'}
         onChange={() => setAmountType('separate')}
         className="accent-brand-600"
       />
       <span className="text-sm text-gray-700">Entrate/Uscite separate</span>
     </label>
   </div>
   ```

9. **Style footer buttons:**
   ```jsx
   <div className="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-200">
     <button
       className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
       onClick={onCancel}
     >
       Annulla
     </button>
     <button
       className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-2"
       onClick={handleConfirm}
       disabled={!isValid}
     >
       <Check size={16} /> Importa file
     </button>
   </div>
   ```

10. **Remove wizard-form, wizard-field class names** — replace with Tailwind utilities
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { ModalShell } from '../ui'`
    - File contains `<ModalShell title="Importa transazioni" onClose={onCancel} size="lg">`
    - File does NOT contain `className="modal-overlay"`
    - File does NOT contain `className="wizard-form"`
    - File does NOT contain `className="wizard-field"`
    - File contains `focus:ring-brand-500` (form focus styling)
    - File contains `disabled:opacity-50` (disabled button state)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ImportWizard migrated to ModalShell with Tailwind form styling</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CategoryManager to ModalShell</name>
  <files>src/components/modals/CategoryManager.jsx</files>
  <read_first>
    - src/components/modals/CategoryManager.jsx (current implementation — 167 lines)
    - src/components/ui/ModalShell.jsx (API)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (styling)
    - .planning/phases/06-modals-redesign/06-RESEARCH.md (Pitfall 6 — internal styling classes)
  </read_first>
  <action>
Refactor CategoryManager.jsx:

1. **Add import:** `import { ModalShell } from '../ui';`

2. **Replace outer wrapper:** Remove modal-overlay/modal wrapper, use:
   ```jsx
   <ModalShell title="Gestione Categorie" onClose={onClose} size="lg">
   ```

3. **Style category grid container:**
   ```jsx
   <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
   ```

4. **Style each category card:**
   ```jsx
   <div className="p-4 bg-gray-50 rounded-lg border border-gray-200">
     <div className="flex items-center justify-between mb-3">
       <h4 className="font-medium text-gray-800">{category.name}</h4>
       <button
         onClick={() => onDeleteCategory(category.name)}
         className="p-1 text-gray-400 hover:text-red-600 hover:bg-red-50 rounded transition-colors"
         title="Elimina categoria"
       >
         <Trash2 size={16} />
       </button>
     </div>
     
     {/* Keywords */}
     <div className="flex flex-wrap gap-2 mb-3">
       {category.keywords.map(keyword => (
         <span key={keyword} className="inline-flex items-center gap-1 px-2 py-1 bg-white border border-gray-200 rounded text-sm text-gray-700">
           {keyword}
           <button
             onClick={() => onRemoveKeyword(category.name, keyword)}
             className="text-gray-400 hover:text-red-500"
           >
             <X size={12} />
           </button>
         </span>
       ))}
     </div>
     
     {/* Add keyword input */}
     <div className="flex gap-2">
       <input
         type="text"
         placeholder="Nuova keyword"
         className="flex-1 px-2 py-1 text-sm border border-gray-200 rounded focus:outline-none focus:ring-1 focus:ring-brand-500"
         onKeyDown={(e) => {
           if (e.key === 'Enter' && e.target.value.trim()) {
             onAddKeyword(category.name, e.target.value.trim());
             e.target.value = '';
           }
         }}
       />
     </div>
   </div>
   ```

5. **Style add category section:**
   ```jsx
   <div className="flex gap-3 mb-6">
     <input
       type="text"
       value={newCategoryName}
       onChange={e => setNewCategoryName(e.target.value)}
       placeholder="Nome nuova categoria"
       className="flex-1 px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-brand-500"
     />
     <button
       onClick={handleAddCategory}
       disabled={!newCategoryName.trim()}
       className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-2"
     >
       <Plus size={16} /> Aggiungi categoria
     </button>
   </div>
   ```

6. **Style recategorize button (if categoriesChanged):**
   ```jsx
   {categoriesChanged && (
     <div className="p-4 bg-amber-50 border border-amber-200 rounded-lg mb-6">
       <p className="text-amber-800 text-sm mb-2">
         Hai modificato le categorie. Vuoi ricategorizzare le transazioni esistenti?
       </p>
       <button
         onClick={onRecategorize}
         className="bg-amber-600 hover:bg-amber-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
       >
         Ricategorizza transazioni
       </button>
     </div>
   )}
   ```

7. **Remove all old class names:** `.categories-grid`, `.category-card`, `.keyword-tag`, `.btn-delete`, `.search-input`
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { ModalShell } from '../ui'`
    - File contains `<ModalShell title="Gestione Categorie" onClose={onClose} size="lg">`
    - File does NOT contain `className="modal-overlay"`
    - File does NOT contain `className="categories-grid"`
    - File does NOT contain `className="category-card"`
    - File contains `grid grid-cols-1 md:grid-cols-2 gap-4`
    - File contains `focus:ring-brand-500`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>CategoryManager migrated to ModalShell with grid layout and Tailwind-styled category cards</done>
</task>

<task type="auto">
  <name>Task 3: Migrate SyncSettings to ModalShell</name>
  <files>src/components/modals/SyncSettings.jsx</files>
  <read_first>
    - src/components/modals/SyncSettings.jsx (current implementation — 365 lines, complex state)
    - src/components/ui/ModalShell.jsx (API)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (styling)
    - .planning/phases/06-modals-redesign/06-RESEARCH.md (Pitfall 7 — internal confirmDelete state)
  </read_first>
  <action>
Refactor SyncSettings.jsx:

**IMPORTANT:** This modal has internal `confirmDelete` state for Drive backup deletion. Do NOT rename or remove this — it's separate from App.jsx's transaction confirmDelete.

1. **Add import:** `import { ModalShell } from '../ui';`

2. **Replace outer wrapper:** Remove modal-overlay/modal wrapper, use:
   ```jsx
   <ModalShell title="Sincronizzazione Drive" onClose={onClose} size="lg">
   ```

3. **Style authentication status section:**
   ```jsx
   {isAuthenticated ? (
     <div className="p-4 bg-green-50 border border-green-200 rounded-lg mb-6">
       <div className="flex items-center gap-2 text-green-700">
         <Cloud size={20} />
         <span>Connesso come {userInfo?.email}</span>
       </div>
     </div>
   ) : (
     <div className="p-4 bg-gray-50 border border-gray-200 rounded-lg mb-6">
       <p className="text-gray-600 mb-3">Connetti il tuo account Google Drive per sincronizzare i dati.</p>
       <button
         onClick={onSignIn}
         disabled={isLoading}
         className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors disabled:opacity-50 flex items-center gap-2"
       >
         {isLoading ? <Loader2 size={16} className="animate-spin" /> : <Cloud size={16} />}
         Connetti Google Drive
       </button>
     </div>
   )}
   ```

4. **Style backup info section:**
   ```jsx
   {backupInfo && (
     <div className="p-4 bg-gray-50 border border-gray-200 rounded-lg mb-6">
       <h4 className="font-medium text-gray-800 mb-2">Backup esistente</h4>
       <p className="text-sm text-gray-600 mb-3">
         Ultimo aggiornamento: {new Date(backupInfo.modifiedTime).toLocaleString('it-IT')}
       </p>
       <div className="flex gap-3">
         <button
           onClick={onDownload}
           disabled={syncStatus === 'syncing'}
           className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors flex items-center gap-2"
         >
           <Download size={16} /> Ripristina
         </button>
         <button
           onClick={onUpload}
           disabled={syncStatus === 'syncing'}
           className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors flex items-center gap-2"
         >
           {syncStatus === 'syncing' ? <Loader2 size={16} className="animate-spin" /> : <Upload size={16} />}
           Salva backup
         </button>
       </div>
     </div>
   )}
   ```

5. **Style internal delete confirmation (uses local confirmDelete state):**
   ```jsx
   {confirmDelete && (
     <div className="p-4 bg-red-50 border border-red-200 rounded-lg mb-6">
       <p className="text-red-800 mb-3">Sei sicuro di voler eliminare il backup da Google Drive?</p>
       <div className="flex gap-3">
         <button
           onClick={() => setConfirmDelete(false)}
           className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
         >
           Annulla
         </button>
         <button
           onClick={handleDeleteConfirmed}
           className="bg-red-600 hover:bg-red-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
         >
           Elimina backup
         </button>
       </div>
     </div>
   )}
   ```

6. **Style error display:**
   ```jsx
   {error && (
     <div className="p-4 bg-red-50 border border-red-200 rounded-lg mb-6">
       <p className="text-red-700">{error}</p>
     </div>
   )}
   ```

7. **Style footer with disconnect button (if authenticated):**
   ```jsx
   {isAuthenticated && (
     <div className="pt-4 border-t border-gray-200">
       <button
         onClick={onSignOut}
         className="text-red-600 hover:text-red-700 text-sm font-medium"
       >
         Disconnetti account
       </button>
     </div>
   )}
   ```

8. **Remove all old class names** — replace with Tailwind utilities
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { ModalShell } from '../ui'`
    - File contains `<ModalShell title="Sincronizzazione Drive" onClose={onClose} size="lg">`
    - File does NOT contain `className="modal-overlay"`
    - File still contains internal `const [confirmDelete, setConfirmDelete]` (preserved)
    - File contains `bg-green-50` (authenticated status)
    - File contains `bg-red-50` (error/delete confirmation)
    - File contains `animate-spin` (loading spinner)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SyncSettings migrated to ModalShell with auth status sections and preserved internal confirmDelete state</done>
</task>

<task type="auto">
  <name>Task 4: Migrate PayPalEnrichWizard to ModalShell with step animations</name>
  <files>src/components/modals/PayPalEnrichWizard.jsx</files>
  <read_first>
    - src/components/modals/PayPalEnrichWizard.jsx (current implementation — 349 lines)
    - src/components/ui/ModalShell.jsx (API)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (step animation specs)
    - .planning/phases/06-modals-redesign/06-RESEARCH.md (Pitfall 5 — DO NOT fix lint warning)
  </read_first>
  <action>
Refactor PayPalEnrichWizard.jsx:

**CRITICAL:** Do NOT modify the useEffect + setSelectedMatches pattern at line ~200. The pre-existing lint warning must remain.

1. **Add imports:**
   ```jsx
   import { ModalShell } from '../ui';
   import { AnimatePresence, motion } from 'framer-motion';
   ```

2. **Add step animation variants:**
   ```jsx
   const stepVariants = {
     enter: (direction) => ({
       x: direction > 0 ? 30 : -30,
       opacity: 0,
     }),
     center: {
       x: 0,
       opacity: 1,
       transition: { duration: 0.2, ease: 'easeOut' },
     },
     exit: (direction) => ({
       x: direction > 0 ? -30 : 30,
       opacity: 0,
       transition: { duration: 0.2, ease: 'easeOut' },
     }),
   };
   ```

3. **Add direction state** (to track forward/back for animation direction):
   ```jsx
   const [direction, setDirection] = useState(0); // 1 = forward, -1 = back
   ```

4. **Replace outer wrapper:** Remove modal-overlay/modal, use:
   ```jsx
   <ModalShell title="Arricchisci transazioni PayPal" onClose={onCancel} size="lg">
   ```

5. **Wrap step content with AnimatePresence:**
   ```jsx
   <AnimatePresence mode="wait" custom={direction}>
     <motion.div
       key={step}
       custom={direction}
       variants={stepVariants}
       initial="enter"
       animate="center"
       exit="exit"
     >
       {step === 1 && (
         // Step 1 content
       )}
       {step === 2 && (
         // Step 2 content
       )}
       {step === 3 && (
         // Step 3 content
       )}
     </motion.div>
   </AnimatePresence>
   ```

6. **Update navigation handlers:**
   ```jsx
   const goNext = () => {
     setDirection(1);
     setStep(s => s + 1);
   };
   
   const goBack = () => {
     setDirection(-1);
     setStep(s => s - 1);
   };
   ```

7. **Style table container (scrollable):**
   ```jsx
   <div className="border border-gray-200 rounded-lg overflow-hidden mb-6">
     <div className="overflow-y-auto max-h-80">
       <table className="w-full text-sm">
         <thead className="sticky top-0 bg-gray-100 z-10">
           <tr>
             <th className="px-4 py-3 text-left font-medium text-gray-700">Seleziona</th>
             <th className="px-4 py-3 text-left font-medium text-gray-700">Descrizione</th>
             <th className="px-4 py-3 text-right font-medium text-gray-700">Importo</th>
           </tr>
         </thead>
         <tbody className="divide-y divide-gray-200">
           {/* rows */}
         </tbody>
       </table>
     </div>
   </div>
   ```

8. **Style footer navigation buttons:**
   ```jsx
   <div className="flex justify-between pt-4 border-t border-gray-200">
     <button
       onClick={goBack}
       disabled={step === 1}
       className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
     >
       Indietro
     </button>
     <div className="flex gap-3">
       <button
         onClick={onCancel}
         className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
       >
         Annulla
       </button>
       {step < 3 ? (
         <button
           onClick={goNext}
           className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
         >
           Continua
         </button>
       ) : (
         <button
           onClick={handleConfirm}
           className="bg-brand-600 hover:bg-brand-700 text-white rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
         >
           Applica modifiche
         </button>
       )}
     </div>
   </div>
   ```

9. **Verify lint warning preserved:** The existing `react-hooks/set-state-in-effect` warning at line ~200 must still exist after migration. DO NOT add eslint-disable or refactor the effect.
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `import { ModalShell } from '../ui'`
    - File contains `import { AnimatePresence, motion } from 'framer-motion'`
    - File contains `<ModalShell title="Arricchisci transazioni PayPal" onClose={onCancel} size="lg">`
    - File contains `stepVariants` with enter/center/exit
    - File contains `<AnimatePresence mode="wait"`
    - File contains `custom={direction}`
    - File does NOT contain `className="modal-overlay"`
    - File still contains `setSelectedMatches` in a useEffect (lint warning preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>PayPalEnrichWizard migrated to ModalShell with horizontal step slide animations (AnimatePresence)</done>
</task>

<task type="auto">
  <name>Task 5: Extend SettingsView with modal trigger buttons</name>
  <files>src/views/SettingsView.jsx</files>
  <read_first>
    - src/views/SettingsView.jsx (current placeholder — 10 lines)
    - .planning/phases/06-modals-redesign/06-UI-SPEC.md (SettingsView buttons section)
    - .planning/phases/06-modals-redesign/06-RESEARCH.md (Pattern 4: SettingsView buttons)
  </read_first>
  <action>
Replace SettingsView.jsx with:

```jsx
/**
 * Settings view with modal trigger buttons for CategoryManager and SyncSettings
 * @param {Object} props
 * @param {Function} props.onShowCategoryManager - Opens CategoryManager modal
 * @param {Function} props.onShowSyncSettings - Opens SyncSettings modal
 */
export function SettingsView({ onShowCategoryManager, onShowSyncSettings }) {
  return (
    <div className="p-8">
      <h2 className="text-2xl font-semibold text-gray-800 mb-6">Impostazioni</h2>

      <div className="space-y-6">
        <section>
          <h3 className="text-sm font-medium text-gray-500 uppercase tracking-wider mb-3">
            Categorie
          </h3>
          <button
            onClick={onShowCategoryManager}
            className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
          >
            Gestione Categorie
          </button>
        </section>

        <section>
          <h3 className="text-sm font-medium text-gray-500 uppercase tracking-wider mb-3">
            Sincronizzazione
          </h3>
          <button
            onClick={onShowSyncSettings}
            className="bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-lg px-4 py-2 text-sm font-semibold transition-colors"
          >
            Sincronizzazione Drive
          </button>
        </section>
      </div>
    </div>
  );
}
```

This adds:
- `onShowCategoryManager` prop — triggers CategoryManager modal
- `onShowSyncSettings` prop — triggers SyncSettings modal
- Two sections with descriptive headings
- Consistent secondary button styling
  </action>
  <verify>
    <automated>npm run build 2>&1 | Select-String -Pattern "(error|failed)" -NotMatch | Select-Object -First 3</automated>
  </verify>
  <acceptance_criteria>
    - File contains `onShowCategoryManager` in props
    - File contains `onShowSyncSettings` in props
    - File contains `onClick={onShowCategoryManager}`
    - File contains `onClick={onShowSyncSettings}`
    - File contains "Gestione Categorie" button text
    - File contains "Sincronizzazione Drive" button text
    - File contains `bg-gray-100 hover:bg-gray-200` (secondary button style)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SettingsView extended with two modal trigger buttons (CategoryManager, SyncSettings)</done>
</task>

</tasks>

<verification>
After all tasks:

1. **Build check:** `npm run build` — must exit 0
2. **Lint check:** `npm run lint` — must exit 0 (warnings OK, PayPalEnrichWizard warning preserved)
3. **Visual structure check:** All 4 modals:
   - Import ModalShell from '../ui'
   - No .modal-overlay or .modal class names
   - Consistent button styling with Tailwind utilities
4. **SettingsView:** Has onShowCategoryManager and onShowSyncSettings props

Wave 2 complex modals complete. Ready for Plan 04 (App.jsx AnimatePresence wiring).
</verification>

<success_criteria>
- ImportWizard uses ModalShell with size="lg", Tailwind form styling
- CategoryManager uses ModalShell with size="lg", grid layout, keyword management
- SyncSettings uses ModalShell with size="lg", auth status sections, preserved internal confirmDelete
- PayPalEnrichWizard uses ModalShell with size="lg", AnimatePresence step animations, preserved lint warning
- SettingsView has two functional modal trigger buttons
- Build and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-modals-redesign/06-03-SUMMARY.md`
</output>
